:javascript
  if (#{notice.present?} || #{alert.present?}) {
    new Noty({
      type: "#{notice ? 'success' : 'warning'}",
      text: "#{notice || alert}",
      theme: 'nest',
      timeout: 3000
    }).show();
  }
